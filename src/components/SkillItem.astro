<p class="skill-item">
    <slot />
</p>

<style is:inline>
    .skill-item {
        font-size: 20px;
        font-weight: 500;
        text-align: center;
        padding: 16px;
        color: var(--secondaryColorGray);
        background-color: var(--primaryColorOrange);
        cursor: pointer;

        &:hover {
            transform: scale(1.1);
            transition: all 0.2s ease;
        }
    }
</style>
